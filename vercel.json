{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": null,
  "installCommand": "pnpm install --frozen-lockfile=false && pnpm prisma generate && pnpm build",
  "buildCommand": "true",
  "outputDirectory": ".",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "headers": [
    {
      "source": "/docs.json",
      "headers": [{ "key": "Cache-Control", "value": "no-store" }]
    }
  ]
}
