# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:3000

# Must match the backend's APP_CHECK_TOKEN env. The Firebase App Check is mocked
# in this case study; in production this token is exchanged via the Firebase SDK.
VITE_APP_CHECK_TOKEN=replace-me-with-a-random-app-check-token
